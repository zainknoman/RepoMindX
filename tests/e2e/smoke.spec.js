import{test,expect}from'@playwright/test';test('primary navigation exists',async({page})=>{await page.goto('/');await expect(page.getByText('◆ RepoMindX',{exact:true})).toBeVisible();for(const n of ['Dashboard','Explorer','Search','Editor','Analyze','Compare','Transform','Context','Document'])await expect(page.getByRole('button',{name:n})).toBeVisible()})