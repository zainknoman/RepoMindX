import{describe,it,expect}from'vitest';import{parseFile,buildIndex,cycles,buildContext}from'./repository';describe('repository intelligence',()=>{it('parses symbols and imports',()=>{const f=parseFile({path:'src/a.js',text:"import x from './b.js'; function hello(){}"});expect(f.imports).toContain('./b.js');expect(f.symbols[0].name).toBe('hello')});it('builds dependency edges and unresolved imports',()=>{const i=buildIndex([{path:'src/a.js',text:"import x from './b.js'; import y from './missing.js'"},{path:'src/b.js',text:'export const x=1'}]);expect(i.edges).toEqual([{from:'src/a.js',to:'src/b.js'}]);expect(i.unresolved[0].import).toBe('./missing.js')});it('detects dependency cycles',()=>{const i=buildIndex([{path:'src/a.js',text:"import b from './b.js'"},{path:'src/b.js',text:"import a from './a.js'"}]);expect(cycles(i).length).toBeGreaterThan(0)});it('builds portable context',()=>{const i=buildIndex([{path:'src/a.js',text:'export const x=1'}]);const c=buildContext(i,['src/a.js']);expect(c.content).toContain('src/a.js');expect(c.tokens).toBeGreaterThan(0)})})